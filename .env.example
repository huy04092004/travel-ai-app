# Backend - JWT & API keys (optional, defaults work for local dev)
JWT_SECRET=danentang
GEOAPIFY_API_KEY=your_geoapify_key
GEMINI_API_KEY=your_gemini_key
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password

# Frontend - API URL (for Docker: backend is at localhost:3000)
# EXPO_PUBLIC_API_URL is set in docker-compose for frontend service
